[project]
name = "canvas-stream"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests==2.26",
    "toml==0.10",
    "typing-extensions>=4.14.0",
]
